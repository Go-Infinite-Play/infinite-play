---
phase: 02-hero-value-proposition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/constants.ts
  - components/Hero.tsx
autonomous: false
requirements:
  - COPY-01
  - COPY-03

must_haves:
  truths:
    - "A visitor landing on the site understands who Jeremy helps and what he does within 5 seconds"
    - "Above-the-fold content clearly communicates Claude implementation consulting, not generic AI"
    - "Headline and subhead pass the 5-second test: a stranger can articulate what Jeremy does after a glance"
    - "Tone is direct and practical — zero corporate jargon, zero AI buzzwords, zero urgency tactics"
    - "Jeremy's headshot is visible in the hero section"
  artifacts:
    - path: "lib/constants.ts"
      provides: "heroContent object with headline, subhead, CTA labels"
      contains: "heroContent"
    - path: "components/Hero.tsx"
      provides: "Hero section with headshot, headline, subhead, two CTAs"
      contains: "founder-photo"
  key_links:
    - from: "components/Hero.tsx"
      to: "lib/constants.ts"
      via: "import heroContent"
      pattern: "import.*heroContent.*constants"
    - from: "components/Hero.tsx"
      to: "public/founder-photo.jpg"
      via: "next/image src"
      pattern: "founder-photo"
---

<objective>
Rewrite the hero section with real positioning copy and Jeremy's headshot so visitors understand "Claude implementation consulting" within 5 seconds of landing.

Purpose: This is the above-the-fold first impression — the single most important piece of copy on the site. It must immediately communicate who Jeremy helps, what he does, and how to take the next step.
Output: Updated Hero component with real copy sourced from constants, headshot image, and two CTAs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-hero-value-proposition/02-CONTEXT.md

<interfaces>
<!-- Existing animation variants used by Hero.tsx -->
From lib/animations.ts:
```typescript
export const staggerContainer: Variants  // hidden/visible with staggerChildren: 0.05
export const staggerItem: Variants       // hidden/visible with y: 20 fade
export const buttonHover: Variants       // rest/hover/tap with scale
```

From components/ui/button.tsx:
```typescript
// shadcn Button with variant="outline" and default variants
```

From lib/constants.ts (current exports used by other components):
```typescript
export const navigationItems = [...] as const
export const siteConfig = { name, description, tagline } as const
export const contactInfo = { email, linkedin, location } as const
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add heroContent to constants and rewrite Hero component</name>
  <files>lib/constants.ts, components/Hero.tsx</files>
  <read_first>
    - lib/constants.ts (full file — need to see all existing exports to add heroContent without conflicts)
    - components/Hero.tsx (full file — rewriting with new layout)
    - lib/animations.ts (reference staggerContainer, staggerItem, buttonHover signatures)
  </read_first>
  <action>
**Step 1: Add heroContent to lib/constants.ts** (per COPY-01, COPY-03)

Add a new exported `heroContent` object at the top of the file (before navigationItems). Use `as const`:

```typescript
export const heroContent = {
  headline: "I help teams get real value from Claude",
  subhead: "Setup, workflow redesign, and hands-on training for teams that want to actually use Claude — not just talk about it.",
  primaryCta: "Book a Discovery Call",
  secondaryCta: "See What I Offer",
  secondaryCtaHref: "#services",
} as const
```

Headline direction per user decision: "I help [audience] get real value from Claude" — first person, direct, Claude-specific. The word "teams" covers all three audience segments (SMB founders have teams, mid-market teams, knowledge worker teams).

Subhead per user decision: one sentence explaining the 3 services. Lists setup, workflow redesign, and training. Ends with an authentic, slightly opinionated line that sets tone.

BANNED words (per COPY-03 tone decision): "revolutionize", "transform", "unlock", "leverage", "empower", "cutting-edge", "game-changing". Do NOT use any of these in copy.

**Step 2: Rewrite components/Hero.tsx**

Rewrite the Hero component with this structure:
- Two-column layout on desktop (text left, photo right), stacked on mobile (text first, photo below)
- Import `heroContent` from `@/lib/constants`
- Import `Image` from `next/image` for the headshot
- Keep `"use client"` directive (Framer Motion requires it)
- Keep existing animation pattern: `staggerContainer` on wrapper, `staggerItem` on children, `buttonHover` on CTAs
- Hero uses `animate` (not `whileInView`) since it is above the fold — this is already the pattern

Layout specifics:
- Section: `py-16 md:py-24 bg-background` (matches current)
- Container: `max-w-[1200px] mx-auto px-6 md:px-12 lg:px-0`
- Grid: `grid grid-cols-1 lg:grid-cols-2 gap-12 items-center`
- Left column (text): headline h1, subhead p, CTA button row
- Right column (photo): `next/image` with `src="/founder-photo.jpg"`, `alt="Jeremy Olken"`, `width={500}`, `height={500}`, rounded corners (`rounded-2xl`), subtle shadow (`shadow-lg`)
- h1: `font-heading text-[2rem] md:text-[3rem] font-bold leading-[1.1] text-foreground mb-6` (matches Phase 1 design tokens)
- Subhead p: `text-base md:text-lg text-muted-foreground leading-[1.6] max-w-xl mb-8`
- Primary CTA: `<Button>` with `bg-primary text-primary-foreground px-8 py-4 text-base font-semibold`, wrapped in buttonHover motion.div, onClick scrolls to `#contact`
- Secondary CTA: `<Button variant="outline">` with `px-8 py-4 text-base font-semibold`, text from heroContent.secondaryCta, onClick scrolls to heroContent.secondaryCtaHref
- Primary CTA shows ArrowRight icon after text (keep existing pattern)
- Secondary CTA has NO icon (just text)

Keep the existing `scrollToSection` helper function.

Do NOT add any corporate jargon, urgency language, or AI buzzwords anywhere in the component. The copy comes entirely from heroContent in constants — no hardcoded strings in the component except alt text.
  </action>
  <verify>
    <automated>cd /Users/olken/Projects/PERSONAL/infinite-play && npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "heroContent" lib/constants.ts (heroContent object exists in constants)
    - grep -q "I help teams get real value from Claude" lib/constants.ts (headline copy is Claude-specific, first person)
    - grep -q "founder-photo" components/Hero.tsx (headshot image is referenced)
    - grep -q "heroContent" components/Hero.tsx (component imports copy from constants)
    - grep -q "next/image" components/Hero.tsx (using Next.js Image component for photo)
    - grep -q "Book a Discovery Call" lib/constants.ts (primary CTA label per user decision)
    - grep -q "See What I Offer" lib/constants.ts (secondary CTA per user decision)
    - grep -qv "revolutionize\|transform\|unlock\|leverage\|empower\|cutting-edge\|game-changing" components/Hero.tsx (no banned words in component)
    - npm run build succeeds with exit code 0
  </acceptance_criteria>
  <done>
    - heroContent exported from lib/constants.ts with headline, subhead, primaryCta, secondaryCta, secondaryCtaHref
    - Hero.tsx renders two-column layout: text left, headshot right on desktop; stacked on mobile
    - All copy sourced from heroContent — no hardcoded marketing strings in component
    - Headshot visible via next/image with founder-photo.jpg
    - Both CTAs present: "Book a Discovery Call" (primary) and "See What I Offer" (secondary outline)
    - Build passes cleanly
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of hero section</name>
  <files>components/Hero.tsx</files>
  <action>
Human visually verifies the hero section in browser. No automated work in this task — Task 1 did all implementation.

What was built: Hero section rewritten with real positioning copy ("I help teams get real value from Claude"), Jeremy's headshot, and two CTAs. Desktop shows text-left/photo-right layout; mobile stacks text above photo.

How to verify:
1. Run `npm run dev` and open http://localhost:3000
2. Check above-the-fold content:
   - Headline reads "I help teams get real value from Claude" (or close variant)
   - Subhead mentions setup, workflow redesign, and training
   - Jeremy's photo is visible to the right of text (desktop) or below text (mobile)
3. Verify the 5-second test: Can you immediately tell this is Claude implementation consulting?
4. Check tone: No corporate jargon, no AI buzzwords, no urgency tactics
5. Click "Book a Discovery Call" — should scroll to contact/CTA section
6. Click "See What I Offer" — should scroll to services section
7. Resize to mobile width — layout should stack (text above photo)
8. Verify animation: elements should fade in on page load (not on scroll)
  </action>
  <verify>
    <automated>echo "Manual checkpoint — human visual verification"</automated>
  </verify>
  <done>User confirms hero section passes the 5-second test, copy tone is correct, photo displays properly, CTAs scroll to correct sections, and mobile layout stacks correctly.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- `grep -q "heroContent" lib/constants.ts` — heroContent object exists
- `grep -q "founder-photo" components/Hero.tsx` — headshot integrated
- `grep -q "heroContent" components/Hero.tsx` — copy sourced from constants
- Visual: above-the-fold content passes the 5-second test for "Claude implementation consulting"
</verification>

<success_criteria>
- A visitor landing on the site can articulate "this person helps teams use Claude" within 5 seconds
- Hero displays headline, subhead, headshot, and two CTAs (Book a Discovery Call + See What I Offer)
- All copy is direct, first-person, and free of AI hype or corporate jargon
- Copy is centralized in lib/constants.ts heroContent, not hardcoded in component
- Build passes, lint passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-hero-value-proposition/02-01-SUMMARY.md`
</output>
