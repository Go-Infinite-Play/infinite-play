---
phase: 07-booking-contact-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/constants.ts
  - components/CalendlyButton.tsx
  - components/Hero.tsx
  - components/CTA.tsx
  - components/Services.tsx
  - components/ResultsSection.tsx
autonomous: true
requirements: [CONV-01]

must_haves:
  truths:
    - "Book a Discovery Call CTA appears in the hero section and opens Calendly popup"
    - "Book a Discovery Call CTA appears after Services, Results, and in the CTA section"
    - "Calendly popup widget loads on demand, not on page load"
  artifacts:
    - path: "components/CalendlyButton.tsx"
      provides: "Reusable Calendly popup trigger button"
      exports: ["default"]
    - path: "lib/constants.ts"
      provides: "Calendly URL constant"
      contains: "calendlyUrl"
  key_links:
    - from: "components/CalendlyButton.tsx"
      to: "Calendly popup widget"
      via: "window.Calendly.initPopupWidget on click"
      pattern: "Calendly\\.initPopupWidget"
    - from: "components/Hero.tsx"
      to: "components/CalendlyButton.tsx"
      via: "import and render"
      pattern: "CalendlyButton"
---

<objective>
Add Calendly booking integration across the site so visitors can book a discovery call from the hero and after each major section.

Purpose: CONV-01 requires a "Book a Discovery Call" CTA with Calendly integration in the hero and after major sections. Currently the CTA button just scrolls to #contact — it needs to open Calendly.
Output: CalendlyButton component, Calendly script loading, updated Hero/CTA/Services/Results sections with booking CTAs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@components/Hero.tsx
@components/CTA.tsx
@components/Services.tsx
@components/ResultsSection.tsx
@lib/constants.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From lib/constants.ts:
```typescript
export const heroContent = {
  headline: "Your Team Has Claude.\nNow Make It Actually Work.",
  subhead: "...",
  primaryCta: "Book a Discovery Call",
  secondaryCta: "See How It Works",
  secondaryCtaHref: "#how-we-work",
} as const;

export const contactInfo = {
  email: "jeremy@infiniteplay.ai",
  linkedin: "https://www.linkedin.com/in/jeremyolken/",
  location: "Denver, CO"
} as const;
```

From lib/animations.ts:
```typescript
export const buttonHover: Variants;
export const staggerItem: Variants;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CalendlyButton component and add Calendly URL constant</name>
  <read_first>
    - lib/constants.ts (for contactInfo pattern and existing constants style)
    - components/Hero.tsx (for button styling patterns)
    - lib/animations.ts (for buttonHover variant)
  </read_first>
  <files>lib/constants.ts, components/CalendlyButton.tsx</files>
  <action>
1. In lib/constants.ts, add a `calendlyUrl` to the `contactInfo` object:
   ```
   calendly: "https://calendly.com/jeremy-infiniteplay/discovery"
   ```
   This is a placeholder URL — Jeremy will update it when his Calendly is configured (per blocker in STATE.md).

2. Create components/CalendlyButton.tsx as a "use client" component:
   - Accept props: `className?: string`, `children?: React.ReactNode`, `variant?: "primary" | "outline"` (default "primary")
   - On mount, lazily inject the Calendly CSS and JS assets into document.head if not already present:
     - CSS: `https://assets.calendly.com/assets/external/widget.css`
     - JS: `https://assets.calendly.com/assets/external/widget.js`
   - Use a `useEffect` with a ref flag to inject once across all instances
   - On click, call `window.Calendly.initPopupWidget({ url: contactInfo.calendly })` — guard with typeof check
   - Style using the same Button component from `@/components/ui/button` with Framer Motion buttonHover variant wrapping
   - Default children: "Book a Discovery Call" with ArrowRight icon
   - Add TypeScript declaration for `window.Calendly` at the top of the file:
     ```typescript
     declare global {
       interface Window {
         Calendly?: {
           initPopupWidget: (opts: { url: string }) => void
         }
       }
     }
     ```

Per CONV-01: The Calendly popup widget approach (not inline embed) was chosen in CONTEXT.md for lighter, less intrusive UX.
  </action>
  <acceptance_criteria>
    - grep -q "calendly" lib/constants.ts
    - grep -q "CalendlyButton" components/CalendlyButton.tsx
    - grep -q "initPopupWidget" components/CalendlyButton.tsx
    - grep -q "widget.js" components/CalendlyButton.tsx
    - grep -q "use client" components/CalendlyButton.tsx
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>CalendlyButton component exists, compiles without errors, imports Calendly assets lazily, and opens popup on click using contactInfo.calendly URL.</done>
</task>

<task type="auto">
  <name>Task 2: Wire CalendlyButton into Hero, CTA, Services, and Results sections</name>
  <read_first>
    - components/Hero.tsx (current CTA button implementation)
    - components/CTA.tsx (current CTA section)
    - components/Services.tsx (for adding a booking CTA after services)
    - components/ResultsSection.tsx (for adding a booking CTA after results)
  </read_first>
  <files>components/Hero.tsx, components/CTA.tsx, components/Services.tsx, components/ResultsSection.tsx</files>
  <action>
1. **Hero.tsx**: Replace the primary CTA Button that currently does `onClick={() => scrollToSection("#contact")}` with `<CalendlyButton />`. Keep the same styling classes (`bg-primary text-primary-foreground px-8 py-4 text-base font-semibold`). Keep the secondary "See How It Works" button as-is. Import CalendlyButton from `@/components/CalendlyButton`.

2. **CTA.tsx**: Replace the entire "Book a Discovery Call" Button with `<CalendlyButton />`. Keep the same section structure, heading ("Ready to Get Started?"), and subhead. Add two additional contact paths below the CalendlyButton:
   - A line: "Or reach out directly:" followed by
   - `mailto:jeremy@infiniteplay.ai` link styled as text link with Mail icon
   - This satisfies part of SERV-03 (multiple contact paths in CTA section).

3. **Services.tsx**: After the services grid, add a centered CTA block with text "Not sure which service fits?" and a CalendlyButton below it. Use staggerItem variant for animation consistency.

4. **ResultsSection.tsx**: After the results cards, add a centered CTA block with text "Ready to see results like these?" and a CalendlyButton below it. Use staggerItem variant for animation consistency.

Per CONV-01: Booking CTA must appear in hero AND after each major section.
  </action>
  <acceptance_criteria>
    - grep -q "CalendlyButton" components/Hero.tsx
    - grep -q "CalendlyButton" components/CTA.tsx
    - grep -q "CalendlyButton" components/Services.tsx
    - grep -q "CalendlyButton" components/ResultsSection.tsx
    - grep -q "mailto:jeremy@infiniteplay.ai" components/CTA.tsx
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20 && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <done>CalendlyButton renders in Hero (replacing old scroll CTA), CTA section (with direct email link), after Services grid, and after Results cards. All compile and lint cleanly. Per CONV-01, the booking CTA is now in hero and after each major section.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- `npm run lint` passes
- `npm run build` succeeds
- CalendlyButton component exists and uses popup widget approach
- Hero primary CTA uses CalendlyButton (not scroll-to-section)
- CTA section has CalendlyButton + direct email link
- Services section has booking CTA after grid
- Results section has booking CTA after cards
</verification>

<success_criteria>
A visitor can click "Book a Discovery Call" from 4 locations on the page (Hero, Services, Results, CTA), each opening a Calendly popup widget. The CTA section also shows a direct email link as an alternative contact path.
</success_criteria>

<output>
After completion, create `.planning/phases/07-booking-contact-infrastructure/07-01-SUMMARY.md`
</output>
