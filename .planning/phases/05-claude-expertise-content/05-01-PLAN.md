---
phase: 05-claude-expertise-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/constants.ts
  - components/ExpertiseSection.tsx
  - components/WorkflowShowcase.tsx
  - app/page.tsx
autonomous: true
requirements:
  - COPY-04
  - COPY-05
  - TRUST-04

must_haves:
  truths:
    - "Claude product taxonomy is explicitly visible with Chat, Teams, Enterprise, Claude Code, API, and MCP each named and described"
    - "Before/after workflow examples show specific, relatable outcomes from daily Claude usage"
    - "Content demonstrates practitioner-level Claude knowledge a generalist could not produce"
    - "Expertise section is integrated into the page flow between Services/Process and Results/About"
  artifacts:
    - path: "lib/constants.ts"
      provides: "claudeProducts array and workflowExamples array"
      contains: "claudeProducts"
    - path: "components/ExpertiseSection.tsx"
      provides: "Claude product taxonomy grid component"
      min_lines: 40
    - path: "components/WorkflowShowcase.tsx"
      provides: "Before/after workflow examples component"
      min_lines: 40
    - path: "app/page.tsx"
      provides: "Page with ExpertiseSection and WorkflowShowcase integrated"
      contains: "ExpertiseSection"
  key_links:
    - from: "components/ExpertiseSection.tsx"
      to: "lib/constants.ts"
      via: "import claudeProducts"
      pattern: "import.*claudeProducts.*from.*constants"
    - from: "components/WorkflowShowcase.tsx"
      to: "lib/constants.ts"
      via: "import workflowExamples"
      pattern: "import.*workflowExamples.*from.*constants"
    - from: "app/page.tsx"
      to: "components/ExpertiseSection.tsx"
      via: "import and render"
      pattern: "import ExpertiseSection"
    - from: "app/page.tsx"
      to: "components/WorkflowShowcase.tsx"
      via: "import and render"
      pattern: "import WorkflowShowcase"
---

<objective>
Build the Claude expertise and practitioner credibility sections that differentiate Jeremy from generalist AI consultants.

Purpose: Demonstrate deep, specific Claude product knowledge across the full product line (Chat, Teams, Enterprise, Claude Code, API, MCP) and show real before/after workflow examples that prove daily practitioner usage. This is the content that makes a visitor think "this person actually knows Claude" rather than "this person consults about AI tools."

Output: Two new components (ExpertiseSection, WorkflowShowcase) with supporting data constants, integrated into the page between Process and Results.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-claude-expertise-content/05-CONTEXT.md
@lib/constants.ts
@lib/animations.ts
@components/Services.tsx
@components/ResultsSection.tsx
@app/page.tsx

<interfaces>
<!-- Existing animation variants used across all section components -->
From lib/animations.ts:
```typescript
export const sectionFadeIn: Variants
export const staggerContainer: Variants
export const staggerItem: Variants
export const fadeInUp: Variants
```

From components/ui/card.tsx (shadcn Card):
```typescript
export { Card, CardContent, CardFooter, CardHeader, CardTitle }
```

From components/ui/badge.tsx (shadcn Badge):
```typescript
export { Badge }
```

<!-- Pattern: sections use "use client", motion wrappers with sectionFadeIn or staggerContainer,
     max-w-[1200px] or max-w-7xl containers, py-8 md:py-16 spacing,
     data from lib/constants.ts, icons from lucide-react -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Claude product taxonomy and workflow example data to constants</name>
  <read_first>lib/constants.ts</read_first>
  <files>lib/constants.ts</files>
  <action>
Add two new data arrays to the bottom of lib/constants.ts (before the closing of the file), following the existing pattern of typed const arrays.

**claudeProducts array** (per COPY-04 — Claude product expertise taxonomy):
Each entry has: id, name, icon (lucide icon name), category ("individual" | "team" | "developer"), description (1 sentence what it is), howJeremyHelps (1 sentence what Jeremy does with it). All six products must be present:

1. id: "chat", name: "Claude Chat", icon: "MessageSquare", category: "individual", description: "The conversational interface millions use daily for writing, analysis, and problem-solving.", howJeremyHelps: "I teach teams to move beyond basic prompting — custom instructions, Projects for context management, and workflows that replace hours of manual work."
2. id: "teams", name: "Claude for Teams", icon: "Users", category: "team", description: "Shared workspace with team-wide conversations, centralized billing, and admin controls.", howJeremyHelps: "I set up team workspaces with shared prompt libraries, onboard every team member, and build adoption habits that stick past week one."
3. id: "enterprise", name: "Claude Enterprise", icon: "Building2", category: "team", description: "SSO, SCIM provisioning, expanded context windows, and enterprise-grade security for large organizations.", howJeremyHelps: "I design the rollout strategy, configure workspaces by department, and create training programs scaled to hundreds of users."
4. id: "code", name: "Claude Code", icon: "Terminal", category: "developer", description: "An agentic coding tool that lives in your terminal — reads your codebase, runs commands, and writes code alongside you.", howJeremyHelps: "I configure Claude Code for your repo with custom CLAUDE.md instructions, slash commands, and MCP integrations that make it a real team member."
5. id: "api", name: "Claude API", icon: "Code", category: "developer", description: "Direct access to Claude models for building custom applications, automations, and integrations.", howJeremyHelps: "I architect API-powered workflows — from automated document processing to custom internal tools — so Claude works even when nobody is prompting."
6. id: "mcp", name: "MCP (Model Context Protocol)", icon: "Plug", category: "developer", description: "An open standard that connects Claude to your tools, databases, and internal systems.", howJeremyHelps: "I build custom MCP servers that give Claude access to your Notion, Slack, databases, and internal APIs — turning it from a chatbot into an integrated team member."

**workflowExamples array** (per COPY-05 — practitioner credibility):
Each entry has: id, title, context (who/what situation), before (the old way — specific), after (the new way with Claude — specific), timeSaved (a concrete metric), claudeProduct (which product was used). Create 4 examples:

1. id: "weekly-reports", title: "Weekly Status Reports", context: "Operations team at a 50-person company", before: "Manager spends 3 hours every Monday compiling updates from Slack, email, and project tools into a status report", after: "Claude with MCP pulls from Slack and Linear, drafts the report in 10 minutes. Manager reviews and sends in 20 minutes total.", timeSaved: "2.5 hours/week", claudeProduct: "MCP + Claude Chat"
2. id: "code-review", title: "Code Review and Documentation", context: "Engineering team adopting AI-assisted development", before: "Senior devs spend 6+ hours per week reviewing PRs and writing documentation nobody reads", after: "Claude Code reviews PRs against team standards, suggests fixes, and generates documentation that stays in sync with the code.", timeSaved: "4 hours/week per senior dev", claudeProduct: "Claude Code"
3. id: "customer-research", title: "Customer Research Synthesis", context: "Product team at a growing SaaS company", before: "PM manually reads 200+ support tickets and NPS responses, writes a quarterly insights deck over 2 days", after: "Claude analyzes all tickets via API, identifies patterns, and produces a structured insights report with citations. PM refines in 2 hours.", timeSaved: "1.5 days/quarter", claudeProduct: "Claude API"
4. id: "onboarding-docs", title: "New Hire Onboarding", context: "HR team struggling with outdated documentation", before: "New hires get a stale Notion wiki and figure things out by asking teammates, slowing everyone down for 2-3 weeks", after: "Claude-powered onboarding assistant (via MCP) answers questions from live company docs, Slack history, and process wikis. New hires are productive in days.", timeSaved: "1-2 weeks ramp time", claudeProduct: "MCP + Claude Teams"

Add both arrays with `as const` at the end, matching the existing pattern in the file.
  </action>
  <acceptance_criteria>
    - lib/constants.ts contains `claudeProducts` with exactly 6 entries covering Chat, Teams, Enterprise, Claude Code, API, MCP
    - lib/constants.ts contains `workflowExamples` with exactly 4 entries each having before/after/timeSaved fields
    - Each claudeProduct entry has id, name, icon, category, description, howJeremyHelps fields
    - Each workflowExample entry has id, title, context, before, after, timeSaved, claudeProduct fields
  </acceptance_criteria>
  <verify>
    <automated>grep -c "claudeProducts\|workflowExamples" lib/constants.ts | grep -q "2" && grep -c '"chat"\|"teams"\|"enterprise"\|"code"\|"api"\|"mcp"' lib/constants.ts | grep -q "6" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Both claudeProducts (6 items) and workflowExamples (4 items) arrays exist in constants.ts with all required fields populated with specific, practitioner-level content.</done>
</task>

<task type="auto">
  <name>Task 2: Create ExpertiseSection and WorkflowShowcase components</name>
  <read_first>components/Services.tsx, components/ResultsSection.tsx, lib/animations.ts, lib/constants.ts</read_first>
  <files>components/ExpertiseSection.tsx, components/WorkflowShowcase.tsx</files>
  <action>
Create two new "use client" components following the exact patterns established in existing section components (motion wrappers, staggerContainer/staggerItem, lucide icons, data from constants).

**ExpertiseSection.tsx** — Claude product taxonomy grid (addresses COPY-04):
- Section id="expertise", className="py-8 md:py-16 bg-background"
- Container: max-w-[1200px] mx-auto px-6 md:px-12 lg:px-0
- Section header: h2 "Deep Claude Expertise" with subheading "I don't just consult about AI. I work with the full Claude product line every day — and I'll set your team up on exactly the right tools."
- Render claudeProducts in a 3-column grid (grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6)
- Each product card uses shadcn Card component with:
  - Icon in a primary/10 rounded-lg container (same pattern as Services and ProblemSection)
  - Product name as CardTitle
  - Category as a Badge (variant="secondary") showing "Individual" / "Team" / "Developer"
  - Description paragraph in text-muted-foreground
  - howJeremyHelps paragraph in text-foreground with slightly different styling (perhaps preceded by a small label "How I help:" in text-sm font-semibold)
- Use sectionFadeIn on outer motion.section, staggerContainer + staggerItem on the grid
- Import icons: MessageSquare, Users, Building2, Terminal, Code, Plug from lucide-react
- Create an iconMap object (same pattern as Services.tsx and ProblemSection.tsx)

**WorkflowShowcase.tsx** — Before/after workflow examples (addresses COPY-05 and TRUST-04):
- Section id="workflows", className="py-8 md:py-16 bg-muted"
- Container: max-w-[1200px] mx-auto px-6 md:px-12 lg:px-0
- Section header: h2 "How Teams Actually Use Claude" with subheading "Not hypothetical. These are the kinds of workflows I build with clients every week."
- Render workflowExamples in a 2-column grid (grid-cols-1 md:grid-cols-2 gap-6 md:gap-8)
- Each workflow card uses shadcn Card with:
  - CardHeader: title and context (context in text-sm text-muted-foreground italic)
  - CardContent with a visual before/after layout:
    - "Before" block: text with strikethrough styling (text-muted-foreground line-through) — same pattern as ResultsSection
    - A downward arrow divider (ArrowDown icon centered, text-muted-foreground)
    - "After" block: text in text-foreground font-medium
  - CardFooter: two items side by side — Badge with timeSaved (variant="secondary") and a small text showing claudeProduct used
- Use sectionFadeIn on outer motion.section, staggerContainer + staggerItem on the grid
- Import ArrowDown from lucide-react

Both components must:
- Be "use client" directives at top
- Use motion from framer-motion
- Import animations from @/lib/animations
- Import data from @/lib/constants
- Follow the exact Tailwind class patterns from existing sections (font-heading for headings, text-[1.25rem] md:text-[1.5rem] for h2s, etc.)
  </action>
  <acceptance_criteria>
    - components/ExpertiseSection.tsx exists as a "use client" component rendering a 6-item Claude product grid
    - components/WorkflowShowcase.tsx exists as a "use client" component rendering 4 before/after workflow cards
    - Both import from @/lib/constants and @/lib/animations
    - Both use motion wrappers with staggerContainer/staggerItem
    - Both use shadcn Card components
    - ExpertiseSection shows all 6 Claude products with names, descriptions, and "How I help" text
    - WorkflowShowcase shows before/after with strikethrough pattern and timeSaved badges
  </acceptance_criteria>
  <verify>
    <automated>grep -l "claudeProducts" components/ExpertiseSection.tsx && grep -l "workflowExamples" components/WorkflowShowcase.tsx && grep -l "use client" components/ExpertiseSection.tsx components/WorkflowShowcase.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>ExpertiseSection renders the Claude product taxonomy grid with all 6 products. WorkflowShowcase renders 4 before/after workflow examples with time-saved metrics. Both follow established component patterns.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate expertise sections into page and update navigation</name>
  <read_first>app/page.tsx, lib/constants.ts (navigationItems)</read_first>
  <files>app/page.tsx, lib/constants.ts</files>
  <action>
**app/page.tsx changes:**
- Add imports for ExpertiseSection and WorkflowShowcase at the top
- Insert both components in the page between Process and ResultsSection:
  ```
  <Process />
  <ExpertiseSection />
  <WorkflowShowcase />
  <ResultsSection />
  ```
  This places expertise content after the "how we work" explanation and before results/social proof — the natural position in the buyer journey (you've seen what I offer, now see that I actually know this stuff, then see results).

**lib/constants.ts navigation update:**
- Add { href: "#expertise", label: "Expertise" } to navigationItems array, inserting it after "Services" and before "About":
  ```
  { href: "#how-we-work", label: "How We Work" },
  { href: "#services", label: "Services" },
  { href: "#expertise", label: "Expertise" },
  { href: "#about", label: "About" },
  ```

After both changes, run `npm run build` to verify no TypeScript or build errors.
  </action>
  <acceptance_criteria>
    - app/page.tsx imports and renders ExpertiseSection and WorkflowShowcase
    - ExpertiseSection appears after Process and before ResultsSection in the JSX
    - WorkflowShowcase appears after ExpertiseSection and before ResultsSection
    - navigationItems in lib/constants.ts includes an "Expertise" entry with href "#expertise"
    - npm run build succeeds with no errors
  </acceptance_criteria>
  <verify>
    <automated>grep "ExpertiseSection" app/page.tsx && grep "WorkflowShowcase" app/page.tsx && grep "expertise" lib/constants.ts && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Both expertise components are integrated into the page in the correct position (after Process, before Results). Navigation includes "Expertise" link. Build passes cleanly.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. All 6 Claude products (Chat, Teams, Enterprise, Claude Code, API, MCP) are visible in the expertise section
3. All 4 before/after workflow examples render with strikethrough-before and highlighted-after pattern
4. Navigation "Expertise" link scrolls to the expertise section
5. Section ordering in page.tsx: Hero > TrustBar > ProblemSection > Services > Process > ExpertiseSection > WorkflowShowcase > ResultsSection > About > CTA
</verification>

<success_criteria>
- Claude product taxonomy is explicitly visible with all 6 products named, described, and contextualized with how Jeremy helps
- Before/after workflow examples show specific, measurable outcomes (time saved, process improved)
- Content reads as practitioner-level — specific product names, realistic scenarios, concrete metrics
- Sections integrate seamlessly with existing design system (same animations, spacing, card patterns)
- Build passes, no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-claude-expertise-content/05-01-SUMMARY.md`
</output>
