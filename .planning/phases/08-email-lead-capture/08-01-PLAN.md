---
phase: 08-email-lead-capture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/schemas.ts
  - app/api/lead-magnet/route.ts
  - app/api/contact/route.ts
  - app/api/submit-email/route.ts
  - components/LeadMagnet.tsx
  - app/page.tsx
  - package.json
autonomous: true
requirements: [CONV-04, DSGN-06]
user_setup:
  - service: resend
    why: "Transactional email delivery for lead confirmation and Jeremy notification"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend Dashboard (resend.com) -> API Keys -> Create API Key"
    dashboard_config:
      - task: "Verify sending domain"
        location: "Resend Dashboard -> Domains -> Add infiniteplay.ai (or use onboarding@resend.dev for testing)"

must_haves:
  truths:
    - "Visitor can enter their email to receive the Claude Setup Checklist"
    - "After submitting, visitor sees a confirmation message"
    - "Visitor receives a confirmation email via Resend after submitting"
    - "Jeremy receives a notification email with lead details for every submission"
    - "Contact form submissions also trigger a notification email to Jeremy"
  artifacts:
    - path: "lib/schemas.ts"
      provides: "leadMagnetSchema with email validation"
      contains: "leadMagnetSchema"
    - path: "app/api/lead-magnet/route.ts"
      provides: "POST handler: validates email, saves to Airtable, sends confirmation + notification via Resend"
      exports: ["POST"]
    - path: "components/LeadMagnet.tsx"
      provides: "Inline email capture section with lead magnet value prop"
      min_lines: 40
  key_links:
    - from: "components/LeadMagnet.tsx"
      to: "/api/lead-magnet"
      via: "fetch POST on form submit"
      pattern: "fetch.*api/lead-magnet"
    - from: "app/api/lead-magnet/route.ts"
      to: "resend"
      via: "Resend SDK send()"
      pattern: "resend\\.emails\\.send"
    - from: "app/api/contact/route.ts"
      to: "resend"
      via: "Resend SDK send()"
      pattern: "resend\\.emails\\.send"
---

<objective>
Build email lead capture with a Claude Setup Checklist lead magnet and Resend email integration for confirmation and notification emails.

Purpose: Create a secondary conversion path for visitors not ready to book -- they exchange their email for a practical resource, and Jeremy gets notified of every lead.
Output: LeadMagnet section component, lead-magnet API route with Resend emails, notification emails on contact form submissions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-email-lead-capture/08-CONTEXT.md

@lib/schemas.ts
@app/api/submit-email/route.ts
@app/api/contact/route.ts
@components/EmailForm.tsx
@components/CTA.tsx
@app/page.tsx
@lib/constants.ts
@lib/animations.ts
@package.json

<interfaces>
<!-- Key types and contracts the executor needs. -->

From lib/schemas.ts:
```typescript
export const contactFormSchema = z.object({
  name: z.string().min(2, "Name must be at least 2 characters"),
  email: z.string().email("Please enter a valid email address"),
  company: z.string().optional(),
  serviceInterest: z.enum([...], { error: "Please select a service" }),
  message: z.string().min(10).max(2000),
})
export type ContactFormData = z.infer<typeof contactFormSchema>
export const serviceOptions = [...] as const
```

From lib/animations.ts (reuse these):
```typescript
export const sectionFadeIn: Variants
export const staggerContainer: Variants
export const staggerItem: Variants
export const buttonHover: Variants
```

From lib/constants.ts:
```typescript
export const contactInfo = {
  email: "jeremy@infiniteplay.ai",
  linkedin: "...",
  location: "Denver, CO",
}
```

Existing API pattern (from app/api/contact/route.ts):
```typescript
import { NextRequest, NextResponse } from "next/server"
import Airtable from "airtable"
// getAirtableBase() helper, safeParse validation, structured JSON responses
```

Existing EmailForm pattern (from components/EmailForm.tsx):
```typescript
// "use client", useState for email/status/message, fetch POST, motion.button with buttonHover,
// success/error states with CheckCircle/AlertCircle icons, 5-second timeout reset
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resend integration, lead magnet schema, and API routes</name>
  <read_first>
    - package.json (current deps)
    - lib/schemas.ts (extend with leadMagnetSchema)
    - app/api/submit-email/route.ts (existing email route to understand Airtable pattern)
    - app/api/contact/route.ts (add Resend notification here too)
    - lib/constants.ts (contactInfo.email for notification recipient)
  </read_first>
  <files>
    package.json
    lib/schemas.ts
    app/api/lead-magnet/route.ts
    app/api/contact/route.ts
  </files>
  <action>
    1. Install resend: `npm install resend`

    2. Add leadMagnetSchema to lib/schemas.ts (per CONV-04):
       - Add `export const leadMagnetSchema = z.object({ email: z.string().email("Please enter a valid email address") })`
       - Add `export type LeadMagnetData = z.infer<typeof leadMagnetSchema>`
       - Do NOT modify existing contactFormSchema or serviceOptions

    3. Create app/api/lead-magnet/route.ts (per CONV-04 + DSGN-06):
       - Import `Resend` from "resend", `leadMagnetSchema` from "@/lib/schemas", `Airtable` from "airtable"
       - Initialize Resend: `const resend = new Resend(process.env.RESEND_API_KEY)`
       - Use same getAirtableBase() helper pattern as contact/route.ts
       - Use table name: `process.env.AIRTABLE_TABLE_NAME || "Email Capture"` (same as existing submit-email route)
       - POST handler flow:
         a. Parse body, validate with leadMagnetSchema.safeParse()
         b. Save to Airtable "Email Capture" table with fields: { Email: email, Source: "Lead Magnet" }
         c. Send confirmation email to the lead via resend.emails.send():
            - from: "Jeremy at Infinite Play <jeremy@infiniteplay.ai>" (or "onboarding@resend.dev" if domain not verified -- add a comment noting this)
            - to: the submitted email
            - subject: "Your Claude Setup Checklist"
            - html: Plain HTML email with:
              - Greeting: "Thanks for downloading the Claude Setup Checklist!"
              - Brief paragraph: "Here's what you'll find inside: a step-by-step guide to setting up Claude for your team, covering workspace configuration, prompt templates, and integration patterns."
              - A note: "I'll also send you occasional Claude tips -- practical stuff I learn from helping teams adopt Claude every day."
              - Sign-off: "-- Jeremy Olken, Infinite Play"
              - Inline CSS only, no external stylesheets. Keep it simple and readable.
         d. Send notification email to Jeremy via resend.emails.send():
            - from: "Infinite Play Leads <jeremy@infiniteplay.ai>" (or "onboarding@resend.dev")
            - to: "jeremy@infiniteplay.ai"
            - subject: "New lead magnet download"
            - html: Simple notification with the lead's email address and timestamp
         e. Return { success: true, message: "Check your email for the checklist!" }
         f. Wrap in try/catch, console.error on failure, return 500 with { error: string }
       - Set `const RESEND_FROM = process.env.RESEND_FROM_EMAIL || "onboarding@resend.dev"` at the top so it is easy to switch when domain is verified

    4. Update app/api/contact/route.ts to add Jeremy notification email (per DSGN-06):
       - Import `Resend` from "resend"
       - Initialize Resend: `const resend = new Resend(process.env.RESEND_API_KEY)`
       - After successful Airtable write, send notification to Jeremy:
         - from: RESEND_FROM (same pattern as lead-magnet route)
         - to: "jeremy@infiniteplay.ai"
         - subject: `New contact form: ${name}`
         - html: Simple notification with name, email, company, service interest, and message
       - Do NOT fail the whole request if the email fails -- wrap resend.emails.send() in its own try/catch and console.error if it fails (the Airtable save is the critical path)
       - Keep all existing validation, Airtable write, and response logic unchanged
  </action>
  <acceptance_criteria>
    - lib/schemas.ts contains "leadMagnetSchema"
    - lib/schemas.ts contains "LeadMagnetData"
    - app/api/lead-magnet/route.ts exists and exports POST
    - app/api/lead-magnet/route.ts contains "resend.emails.send"
    - app/api/lead-magnet/route.ts contains "leadMagnetSchema"
    - app/api/contact/route.ts contains "resend.emails.send"
    - app/api/contact/route.ts contains "Resend" import
    - package.json contains "resend"
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run build</automated>
  </verify>
  <done>
    - leadMagnetSchema exports from lib/schemas.ts
    - /api/lead-magnet POST validates email, saves to Airtable, sends confirmation email to lead, sends notification to Jeremy
    - /api/contact POST now also sends notification email to Jeremy after successful Airtable save
    - Build passes with no type errors
  </done>
</task>

<task type="auto">
  <name>Task 2: LeadMagnet section component and page integration</name>
  <read_first>
    - components/EmailForm.tsx (visual patterns to follow)
    - components/CTA.tsx (placement context -- LeadMagnet goes before CTA)
    - app/page.tsx (current section order)
    - lib/animations.ts (reuse sectionFadeIn, staggerContainer, staggerItem, buttonHover)
    - lib/schemas.ts (use leadMagnetSchema for client validation after Task 1)
  </read_first>
  <files>
    components/LeadMagnet.tsx
    app/page.tsx
  </files>
  <action>
    1. Create components/LeadMagnet.tsx (per CONV-04):
       - "use client" directive
       - Import useState from "react", motion from "framer-motion"
       - Import Mail, ArrowRight, CheckCircle, AlertCircle, FileText from "lucide-react"
       - Import sectionFadeIn, staggerContainer, staggerItem, buttonHover from "@/lib/animations"
       - Component structure:
         a. Section wrapper: motion.section with id="lead-magnet", sectionFadeIn variants, whileInView animation, py-16 md:py-24 bg-background
         b. Inner container: max-w-[1200px] mx-auto px-6 md:px-12 lg:px-0
         c. Two-column layout (lg:grid lg:grid-cols-2 lg:gap-16, stacks on mobile):
            - LEFT column (value prop):
              - Small label: "Free Resource" in text-xs uppercase tracking-wider text-primary font-semibold
              - Heading: "The Claude Setup Checklist" in font-heading text-[1.25rem] md:text-[1.5rem] font-semibold
              - Description paragraph: "A practical, step-by-step guide to setting up Claude for your team. Covers workspace configuration, prompt templates, integration patterns, and the mistakes I see teams make in their first month."
              - 3-4 bullet points with FileText icon:
                - "Workspace setup and team permissions"
                - "Prompt template library to start with"
                - "Integration patterns for common tools"
                - "First-month pitfalls and how to avoid them"
              - Text style: text-muted-foreground text-base leading-[1.6]
            - RIGHT column (form):
              - Simple card-style container: bg-muted rounded-xl p-8
              - Heading inside card: "Get the checklist" text-lg font-medium
              - Subtext: "Enter your email and I'll send it right over." text-sm text-muted-foreground mb-4
              - Email input: same styling pattern as EmailForm.tsx (pl-12 with Mail icon, rounded-lg, border-border, bg-background/50)
              - Submit button: motion.button with buttonHover variants, bg-primary text-primary-foreground, text "Send Me the Checklist" with ArrowRight icon
              - State management: useState for email, status ("idle" | "loading" | "success" | "error"), message
              - handleSubmit: fetch POST to "/api/lead-magnet" with { email }, same pattern as EmailForm.tsx
              - Success state: show CheckCircle + "Check your email for the checklist!" message, clear input
              - Error state: show AlertCircle + error message
              - 5-second timeout reset on success/error (same pattern as EmailForm.tsx)
              - Privacy note below form: "No spam, ever. Unsubscribe anytime." text-xs text-muted-foreground

    2. Update app/page.tsx:
       - Import LeadMagnet from "@/components/LeadMagnet"
       - Place LeadMagnet BETWEEN About and CTA in the JSX:
         ```
         <About />
         <LeadMagnet />
         <CTA />
         ```
       - This positions the lead magnet as a softer conversion option before the direct CTA
  </action>
  <acceptance_criteria>
    - components/LeadMagnet.tsx exists
    - components/LeadMagnet.tsx contains "use client"
    - components/LeadMagnet.tsx contains "api/lead-magnet"
    - components/LeadMagnet.tsx contains "Claude Setup Checklist"
    - components/LeadMagnet.tsx contains "buttonHover"
    - components/LeadMagnet.tsx contains "id=\"lead-magnet\""
    - app/page.tsx contains "LeadMagnet"
    - app/page.tsx imports from "@/components/LeadMagnet"
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run build</automated>
  </verify>
  <done>
    - LeadMagnet section renders with lead magnet value prop and inline email form
    - Email form submits to /api/lead-magnet
    - LeadMagnet appears between About and CTA on the page
    - Success and error states work with proper feedback messages
    - Build passes with no errors
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npx tsc --noEmit` -- no type errors
2. `npm run lint` -- no lint warnings
3. `npm run build` -- all routes compile successfully
4. Grep checks:
   - `grep -r "leadMagnetSchema" lib/schemas.ts` -- schema exists
   - `grep -r "resend.emails.send" app/api/lead-magnet/route.ts` -- confirmation + notification emails
   - `grep -r "resend.emails.send" app/api/contact/route.ts` -- notification on contact form
   - `grep -r "LeadMagnet" app/page.tsx` -- component integrated
   - `grep -r "api/lead-magnet" components/LeadMagnet.tsx` -- form wired to API
</verification>

<success_criteria>
- Email capture form renders on page with "Claude Setup Checklist" value proposition
- Form submits email to /api/lead-magnet which validates, saves to Airtable, and sends two emails via Resend
- Contact form submissions (/api/contact) now also send a notification email to Jeremy
- All TypeScript compiles, lint passes, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/08-email-lead-capture/08-01-SUMMARY.md`
</output>
