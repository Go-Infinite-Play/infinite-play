---
phase: 03-audience-problem-messaging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/constants.ts
  - components/ProblemSection.tsx
autonomous: true
requirements:
  - COPY-02
  - SERV-05
must_haves:
  truths:
    - "SMB founders see copy that names their situation — small team, bought Claude, nobody using it"
    - "Mid-market teams see copy that names their situation — multiple departments, inconsistent adoption, no internal champion"
    - "Knowledge workers see copy that names their situation — individual professionals losing competitive edge to AI-fluent peers"
    - "Each audience card has a distinct CTA linking toward services section"
  artifacts:
    - path: "lib/constants.ts"
      provides: "audienceSegments array with three segment objects"
      contains: "audienceSegments"
    - path: "components/ProblemSection.tsx"
      provides: "Audience routing section with three cards"
      contains: "audienceSegments"
  key_links:
    - from: "components/ProblemSection.tsx"
      to: "lib/constants.ts"
      via: "import { audienceSegments }"
      pattern: "import.*audienceSegments.*from.*@/lib/constants"
    - from: "components/ProblemSection.tsx"
      to: "#services"
      via: "CTA link on each card"
      pattern: "href.*#services"
---

<objective>
Rewrite the Problem section into an audience-routing section where SMB founders, mid-market teams, and knowledge workers each see a card that speaks to their specific situation and pain point, using language each persona would use themselves.

Purpose: Visitors self-identify with a specific audience segment and feel understood before seeing services — this is the bridge between "what Jeremy does" (hero) and "what he offers" (services).
Output: Updated constants with audience segment data, rewritten ProblemSection component with three audience cards.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-audience-problem-messaging/03-CONTEXT.md

@lib/constants.ts
@components/ProblemSection.tsx
@components/ui/card.tsx
@lib/animations.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From lib/constants.ts (current problemPoints to be replaced):
```typescript
export const problemPoints = [
  { title: string, description: string, icon: string },
  ...
] as const
```

From lib/animations.ts (reuse these):
```typescript
export const sectionFadeIn: Variants
export const staggerContainer: Variants
export const staggerItem: Variants
export const buttonHover: Variants
```

From components/ui/card.tsx (available exports):
```typescript
export { Card, CardHeader, CardFooter, CardTitle, CardAction, CardDescription, CardContent }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define audience segment data in constants</name>
  <files>lib/constants.ts</files>
  <read_first>
    - lib/constants.ts (full file — understand existing pattern for data objects)
    - .planning/phases/03-audience-problem-messaging/03-CONTEXT.md (decisions on segments and tone)
  </read_first>
  <action>
In `lib/constants.ts`, replace the existing `problemPoints` export with a new `audienceSegments` export. Keep the same `as const` pattern used by all other exports in this file.

Define three audience segment objects in an `audienceSegments` array:

**Segment 1 — SMB Founders (5-50 people)**
- id: "smb-founders"
- label: "Small Teams & Founders"
- icon: "Building2"
- problem: "You bought Claude for your team. A few people tried it for a week, then went back to doing things the old way."
- painPoint: "You know there is value here, but nobody has time to figure out where Claude actually fits into your workflows."
- cta: "See how I help small teams"
- ctaHref: "#services"

**Segment 2 — Mid-Market Teams (50-500)**
- id: "mid-market-teams"
- label: "Growing Companies"
- icon: "Users"
- problem: "Some departments are using Claude daily. Others have not touched it. There is no shared playbook and no internal champion."
- painPoint: "You need someone to build the bridge between early adopters and the rest of the organization — without a six-month consulting engagement."
- cta: "See how I help growing teams"
- ctaHref: "#services"

**Segment 3 — Knowledge Workers (lawyers, consultants, agencies)**
- id: "knowledge-workers"
- label: "Independent Professionals"
- icon: "Briefcase"
- problem: "You see peers shipping faster and producing more with AI. You have tried Claude a few times but are not getting the results they seem to get."
- painPoint: "You do not need a course or a certification. You need someone to sit with you, look at your actual work, and show you where Claude fits."
- cta: "See how I help professionals"
- ctaHref: "#services"

Tone rules (from CONTEXT.md):
- Problem-first framing: acknowledge the gap between awareness and adoption
- Use language each persona would use themselves
- No banned words: "revolutionize", "transform", "unlock", "leverage", "empower", "cutting-edge", "game-changing"
- Conversational professional, direct, no AI hype

Also add a section intro constant:
```typescript
export const audienceSectionIntro = {
  heading: "Who I Work With",
  subheading: "I help three types of people get real, lasting value from Claude.",
} as const
```
  </action>
  <acceptance_criteria>
    - grep "audienceSegments" lib/constants.ts returns matches
    - grep "smb-founders" lib/constants.ts returns a match
    - grep "mid-market-teams" lib/constants.ts returns a match
    - grep "knowledge-workers" lib/constants.ts returns a match
    - grep "audienceSectionIntro" lib/constants.ts returns a match
    - grep "problemPoints" lib/constants.ts returns NO matches (old export removed)
    - grep "as const" lib/constants.ts shows the new exports use as const
  </acceptance_criteria>
  <verify>
    <automated>grep -c "audienceSegments" lib/constants.ts && grep -c "smb-founders" lib/constants.ts && grep -c "mid-market-teams" lib/constants.ts && grep -c "knowledge-workers" lib/constants.ts && grep -c "audienceSectionIntro" lib/constants.ts && ! grep -q "problemPoints" lib/constants.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>lib/constants.ts exports audienceSegments (3 segments) and audienceSectionIntro, with no trace of the old problemPoints export. Each segment has id, label, icon, problem, painPoint, cta, and ctaHref fields.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite ProblemSection as audience-routing cards</name>
  <files>components/ProblemSection.tsx</files>
  <read_first>
    - lib/constants.ts (after Task 1 — get the new audienceSegments shape)
    - components/ProblemSection.tsx (current component to rewrite)
    - components/ui/card.tsx (card component API)
    - lib/animations.ts (available animation variants)
  </read_first>
  <action>
Rewrite `components/ProblemSection.tsx` to render audience-routing cards instead of generic problem points.

**Imports to change:**
- Remove: `HelpCircle, Archive, TrendingDown` from lucide-react
- Add: `Building2, Users, Briefcase, ArrowRight` from lucide-react
- Replace: `problemPoints` import with `audienceSegments, audienceSectionIntro` from `@/lib/constants`

**Icon map:** Update to map the new icon strings:
```typescript
const iconMap = {
  Building2: Building2,
  Users: Users,
  Briefcase: Briefcase,
}
```

**Section structure:**
1. Section wrapper: keep `motion.section` with `sectionFadeIn`, `id="audience"` (new id for nav linking)
2. Section intro: render `audienceSectionIntro.heading` as h2 and `audienceSectionIntro.subheading` as p below it, both centered
3. Card grid: 3-column on lg, 1-column on mobile (same grid pattern as current)
4. Each card renders from `audienceSegments.map()`:
   - Icon in a colored circle (keep existing pattern: `w-10 h-10 bg-primary/10 rounded-lg`)
   - `segment.label` as CardTitle
   - `segment.problem` as first paragraph (text-foreground, slightly emphasized)
   - `segment.painPoint` as second paragraph (text-muted-foreground)
   - CTA link at bottom: `segment.cta` text with ArrowRight icon, styled as an inline link (`text-primary font-medium hover:underline`), href pointing to `segment.ctaHref`

**Animation:** Keep existing staggerContainer/staggerItem pattern for the card grid. Use buttonHover on the CTA link area for subtle hover feedback.

**Styling details:**
- Keep existing max-w-[1200px] container, py-8 md:py-16 spacing
- Cards should have equal height (h-full on Card)
- CTA link at card bottom: use CardFooter or a div at the bottom of CardContent with `mt-auto` to push it down
- Card should use `flex flex-col` to allow the footer to stick to bottom

**Keep "use client" directive on line 1.**

Do NOT use any banned words from CONTEXT.md in any hardcoded strings.
  </action>
  <acceptance_criteria>
    - grep "audienceSegments" components/ProblemSection.tsx returns a match
    - grep "audienceSectionIntro" components/ProblemSection.tsx returns a match
    - grep 'id="audience"' components/ProblemSection.tsx returns a match
    - grep "Building2" components/ProblemSection.tsx returns a match
    - grep "Users" components/ProblemSection.tsx returns a match
    - grep "Briefcase" components/ProblemSection.tsx returns a match
    - grep "ArrowRight" components/ProblemSection.tsx returns a match
    - grep "#services" components/ProblemSection.tsx returns a match
    - grep "problemPoints" components/ProblemSection.tsx returns NO matches
    - npm run build succeeds without errors
  </acceptance_criteria>
  <verify>
    <automated>grep -q "audienceSegments" components/ProblemSection.tsx && grep -q "audienceSectionIntro" components/ProblemSection.tsx && grep -q 'id="audience"' components/ProblemSection.tsx && grep -q "ArrowRight" components/ProblemSection.tsx && grep -q "#services" components/ProblemSection.tsx && ! grep -q "problemPoints" components/ProblemSection.tsx && npm run build 2>&1 | tail -5 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>ProblemSection renders three audience cards from audienceSegments data. Each card shows the segment label, problem statement, pain point, and a CTA linking to #services. The component builds successfully with no errors. Old problemPoints references are completely removed.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `grep -r "problemPoints" lib/ components/` returns no matches (fully removed)
3. `grep "audienceSegments" lib/constants.ts components/ProblemSection.tsx` returns matches in both files
4. Each audience segment id appears in constants: smb-founders, mid-market-teams, knowledge-workers
5. ProblemSection has CTA links pointing to #services for each segment
</verification>

<success_criteria>
- Three distinct audience cards render on the page, each with segment-specific problem language
- Each card has a CTA that routes toward the services section
- No old problemPoints data or imports remain anywhere in the codebase
- Site builds successfully with `npm run build`
</success_criteria>

<output>
After completion, create `.planning/phases/03-audience-problem-messaging/03-01-SUMMARY.md`
</output>
