---
phase: 04-services-engagement-process
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/constants.ts
  - components/Services.tsx
  - components/Process.tsx
autonomous: true
requirements:
  - SERV-01
  - SERV-02
  - SERV-04

must_haves:
  truths:
    - "Visitor sees 4 service tiers with outcome-focused names and starting-from pricing"
    - "Visitor sees a 4-step engagement process (Talk > Plan > Execute > Results) with visual progression"
    - "Each service tier displays who it is for, what the deliverable is, and the price"
    - "A visitor can determine which service fits their situation without needing to ask"
  artifacts:
    - path: "lib/constants.ts"
      provides: "Service tiers with pricing and enriched process steps"
      contains: "startingFrom"
    - path: "components/Services.tsx"
      provides: "Service tier cards with pricing, audience, and deliverables"
      contains: "startingFrom"
    - path: "components/Process.tsx"
      provides: "4-step engagement process visual"
      contains: "processSteps"
  key_links:
    - from: "components/Services.tsx"
      to: "lib/constants.ts"
      via: "import { services }"
      pattern: "services\\.map"
    - from: "components/Process.tsx"
      to: "lib/constants.ts"
      via: "import { processSteps }"
      pattern: "processSteps\\.map"
---

<objective>
Replace placeholder Services and Process sections with real content: 4 service tiers with outcome-focused names, "starting from" pricing, audience fit, and deliverables; plus a polished 4-step engagement process visual.

Purpose: Visitors understand exactly what Jeremy offers, what it costs, and how to start working with him (SERV-01, SERV-02, SERV-04).
Output: Updated constants data and rewritten Services.tsx and Process.tsx components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-services-engagement-process/04-CONTEXT.md

@lib/constants.ts
@lib/animations.ts
@components/Services.tsx
@components/Process.tsx
@components/ui/card.tsx
</context>

<interfaces>
<!-- Existing animation variants used by both components -->
From lib/animations.ts:
```typescript
export const sectionFadeIn: Variants
export const staggerContainer: Variants
export const staggerItem: Variants
```

<!-- Existing shadcn card used for service tiers -->
From components/ui/card.tsx:
```typescript
export { Card, CardContent, CardHeader, CardTitle, CardDescription, CardFooter }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update constants with real service tiers and enriched process data</name>
  <read_first>lib/constants.ts</read_first>
  <files>lib/constants.ts</files>
  <action>
Replace the `services` array in `lib/constants.ts` with 4 service tiers containing full detail. Per SERV-01: outcome-focused names. Per SERV-04: "starting from" pricing. Per CONTEXT.md decisions: each tier shows what you get, what it costs, who it is for.

New `services` array (4 items, typed with `as const`):

1. **Team Workshop**
   - title: "Team Workshop"
   - subtitle: "Get your whole team using Claude effectively in one day"
   - description: "Half-day or full-day hands-on training tailored to your team's actual workflows. Everyone leaves with prompts, templates, and confidence."
   - deliverable: "Custom prompt library + workflow templates for your team"
   - audience: "Teams that bought Claude but haven't seen adoption stick"
   - startingFrom: "$2,000"
   - icon: "GraduationCap"
   - duration: "Half-day or full-day"

2. **Claude Quick-Start**
   - title: "Claude Quick-Start"
   - subtitle: "From zero to productive with Claude in one week"
   - description: "A focused 2-4 hour assessment of your workflows, followed by workspace setup, prompt libraries, and hands-on training so your team is productive immediately."
   - deliverable: "Action plan + configured workspace + prompt library"
   - audience: "Small teams and founders ready to start using Claude now"
   - startingFrom: "$1,500"
   - icon: "Zap"
   - duration: "2-4 hours + follow-up"

3. **Implementation Sprint**
   - title: "Implementation Sprint"
   - subtitle: "Rebuild your core workflows around Claude"
   - description: "A 1-2 week deep engagement where I embed with your team, redesign key processes around Claude, set up the environment, and train everyone on the new workflows."
   - deliverable: "Configured environment + redesigned workflows + trained team"
   - audience: "Growing companies ready to make Claude a real part of how they work"
   - startingFrom: "$5,000"
   - icon: "Code"
   - duration: "1-2 weeks"

4. **Ongoing Advisory**
   - title: "Ongoing Advisory"
   - subtitle: "A Claude expert in your corner, month after month"
   - description: "Monthly check-ins, async support, and continuous optimization as your team's Claude usage matures. New workflows, troubleshooting, and strategy as Anthropic ships new features."
   - deliverable: "Monthly strategy sessions + async support + new workflow builds"
   - audience: "Teams scaling Claude usage who want a dedicated expert on call"
   - startingFrom: "$2,000/mo"
   - icon: "RefreshCw"
   - duration: "Monthly"

Also update the `processSteps` array descriptions to be slightly more specific and action-oriented (keep existing structure — number, title, description, icon):

1. number: "01", title: "Talk", description: "Book a free 30-minute discovery call. I'll learn about your team, your tools, and where Claude fits.", icon: "MessageSquare"
2. number: "02", title: "Plan", description: "I'll put together a custom implementation roadmap — specific Claude workflows mapped to your actual use cases.", icon: "Map"
3. number: "03", title: "Execute", description: "Hands-on setup, training, and workflow integration. I work alongside your team, not from the sidelines.", icon: "Rocket"
4. number: "04", title: "Results", description: "Faster workflows, better output, and a team that's confident using Claude every day.", icon: "CheckCircle"

Do NOT change any other constants in the file. Preserve all existing exports.
  </action>
  <acceptance_criteria>
    - lib/constants.ts exports a `services` array with exactly 4 objects
    - Each service object has fields: title, subtitle, description, deliverable, audience, startingFrom, icon, duration
    - startingFrom values are "$2,000", "$1,500", "$5,000", "$2,000/mo"
    - processSteps array still has 4 items with number, title, description, icon
    - All other constants unchanged
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20 && grep -c "startingFrom" lib/constants.ts</automated>
  </verify>
  <done>lib/constants.ts has 4 service tiers with pricing and enriched process descriptions, TypeScript compiles without errors</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Services and Process components with pricing and richer layout</name>
  <read_first>components/Services.tsx, components/Process.tsx, lib/constants.ts, lib/animations.ts</read_first>
  <files>components/Services.tsx, components/Process.tsx</files>
  <action>
**Services.tsx** — Complete rewrite to display 4 service tiers with full detail. Per SERV-01 and SERV-04.

Structure:
- Section heading: "What I Offer" (h2, centered, same typography pattern as existing: `font-heading text-[1.25rem] md:text-[1.5rem] font-semibold`)
- Subheading: "Clear scope, honest pricing, real outcomes." (text-muted-foreground, centered, below h2)
- Grid: `grid-cols-1 md:grid-cols-2 gap-6 md:gap-8` (2x2 grid on desktop, stack on mobile)
- Each service card uses shadcn `Card` component with:
  - Icon (from lucide-react) in a colored circle (same `w-10 h-10 bg-primary/10 rounded-lg` pattern)
  - Title (CardTitle, font-heading)
  - Subtitle (text-sm text-muted-foreground, italic)
  - Description paragraph (text-base text-muted-foreground)
  - Deliverable line: bold "You get:" prefix followed by the deliverable text
  - Audience line: bold "Best for:" prefix followed by the audience text
  - Price badge at bottom: `startingFrom` displayed in a visually distinct way — use a `<span>` with `text-lg font-semibold text-foreground` for the price, preceded by "Starting from" in smaller muted text
  - Duration shown as a small Badge component: `<Badge variant="secondary">{duration}</Badge>`
- Update the `iconMap` to include `GraduationCap` (import from lucide-react)
- Use existing `sectionFadeIn`, `staggerContainer`, `staggerItem` animation variants
- Keep `"use client"` directive
- Keep `id="services"` for anchor linking
- Section background: `bg-muted` (matches current)

**Process.tsx** — Polish the existing process component. Per SERV-02.

Structure:
- Section heading: "How We Work Together" (replaces "How It Works")
- Subheading: "A straightforward path from first conversation to real results." (text-muted-foreground, centered)
- Keep the existing 4-column grid on lg, 2-col on md, 1-col on mobile
- Add a visual connector between steps on desktop: a horizontal dashed line or arrow between the step circles. Implementation: use a `hidden lg:block` div with a dashed border-top between each step (absolute positioned or flex with separator divs). Keep it simple — a thin dashed line connecting the step icons horizontally.
- Each step card:
  - Badge with step number (keep existing pattern)
  - Icon in circle (keep existing pattern)
  - Title (keep existing pattern)
  - Description (now uses the updated, more specific text from Task 1)
- Keep `"use client"` directive
- Keep `id="process"` for anchor linking
- Section background: `bg-background` (matches current)
- Use existing animation variants

Do NOT add any new npm dependencies. Use only lucide-react icons already available plus GraduationCap.
  </action>
  <acceptance_criteria>
    - Services.tsx renders 4 service cards in a 2x2 grid
    - Each card displays title, subtitle, description, deliverable, audience, startingFrom price, and duration badge
    - "Starting from" text appears with each price value
    - Process.tsx heading reads "How We Work Together"
    - Process.tsx has a subheading below the h2
    - Both components compile without TypeScript errors
    - GraduationCap is imported from lucide-react
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Services section shows 4 tiers with pricing, deliverables, and audience fit. Process section shows polished 4-step engagement flow. Build succeeds.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `grep -c "startingFrom" lib/constants.ts` returns 4 (one per service tier)
3. `grep "Starting from" components/Services.tsx` finds the pricing display pattern
4. `grep "How We Work Together" components/Process.tsx` confirms updated heading
</verification>

<success_criteria>
- 4 service tiers visible with outcome-focused names, pricing, deliverables, and audience fit
- 4-step engagement process visible with Talk > Plan > Execute > Results progression
- A first-time visitor can identify which service tier fits their situation
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-services-engagement-process/04-01-SUMMARY.md`
</output>
