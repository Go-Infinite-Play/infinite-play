---
phase: 09-mobile-responsive-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/Hero.tsx
  - components/ExpertiseSection.tsx
  - components/WorkflowShowcase.tsx
  - components/ContactForm.tsx
  - components/LeadMagnet.tsx
  - components/Services.tsx
  - components/Process.tsx
  - components/CTA.tsx
  - components/CalendlyButton.tsx
autonomous: true
requirements:
  - CONV-02
must_haves:
  truths:
    - "All sections are readable and properly spaced on a 375px wide viewport"
    - "CTAs and form buttons have minimum 44px touch targets on mobile"
    - "No horizontal overflow or content clipping on mobile screens"
    - "Navigation hamburger menu works and drawer covers full height"
    - "Contact form fields are usable on mobile with proper spacing"
    - "Hero image does not overflow or dominate on small screens"
  artifacts:
    - path: "components/Hero.tsx"
      provides: "Mobile-optimized hero with proper image sizing"
      contains: "pt-24"
    - path: "components/ContactForm.tsx"
      provides: "Touch-friendly form with proper mobile spacing"
      contains: "min-h-\\[44px\\]"
    - path: "components/Services.tsx"
      provides: "Single-column service cards on mobile"
      contains: "grid-cols-1"
  key_links:
    - from: "components/Hero.tsx"
      to: "components/CalendlyButton.tsx"
      via: "CTA button sizing on mobile"
      pattern: "min-h-\\[44px\\]|py-3"
    - from: "components/ContactForm.tsx"
      to: "app/api/contact/route.ts"
      via: "Form submission works on mobile"
      pattern: "fetch.*api/contact"
---

<objective>
Audit and polish all components for mobile responsiveness, ensuring every section works perfectly on phone and tablet screens.

Purpose: Fulfill CONV-02 (mobile-responsive design). While components already have basic responsive classes, this plan does a thorough audit to fix spacing, touch targets, overflow issues, and mobile-specific layout concerns across all sections.
Output: All components fully functional and visually polished on mobile devices (375px+).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-mobile-responsive-polish/09-CONTEXT.md

<interfaces>
<!-- All components use Tailwind responsive prefixes (md:, lg:) -->
<!-- max-w-[1200px] mx-auto px-6 md:px-12 lg:px-0 is the standard container pattern -->
<!-- Navigation mobile menu already functional (hamburger + drawer) -->

From components/Hero.tsx:
```typescript
// Grid: grid-cols-1 lg:grid-cols-2
// Image: width={500} height={500} — may need responsive sizing
// Padding: py-16 md:py-24 — needs pt adjustment for fixed nav
// CTAs: flex-col sm:flex-row — already responsive
```

From components/Services.tsx:
```typescript
// Grid: grid-cols-1 md:grid-cols-2
// Container: max-w-7xl mx-auto px-4 — inconsistent with other sections (px-6)
```

From components/ContactForm.tsx:
```typescript
// Form inputs: py-3 px-4 — touch target OK
// Submit button: px-6 py-3 — touch target OK
// Max width: max-w-lg mx-auto — good
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Hero, Services, and Process mobile layout issues</name>
  <read_first>components/Hero.tsx, components/Services.tsx, components/Process.tsx, components/CTA.tsx, components/CalendlyButton.tsx</read_first>
  <files>components/Hero.tsx, components/Services.tsx, components/Process.tsx, components/CTA.tsx, components/CalendlyButton.tsx</files>
  <action>
1. Fix `components/Hero.tsx` mobile issues (per CONV-02):
   - Add `pt-24` to the section (currently `py-16 md:py-24`) to account for fixed navigation bar on all screen sizes. Change to `pt-24 pb-16 md:pt-28 md:pb-24`.
   - The hero Image at width={500} height={500} is too large on mobile. Add responsive classes: `className="rounded-2xl shadow-lg w-full max-w-[320px] sm:max-w-[400px] lg:max-w-[500px] h-auto"` and keep width/height as hints only.
   - Ensure CTA buttons (`CalendlyButton` and outline Button) have adequate touch target. The flex-col sm:flex-row pattern is already good.

2. Fix `components/Services.tsx` container inconsistency (per CONV-02):
   - Change `max-w-7xl mx-auto px-4` to `max-w-[1200px] mx-auto px-6 md:px-12 lg:px-0` to match the standard container pattern used in Hero, ExpertiseSection, LeadMagnet, Footer, etc.
   - This ensures consistent horizontal padding on mobile across all sections.

3. Fix `components/Process.tsx` mobile layout:
   - Read the file first to check current mobile patterns
   - Ensure the process steps have adequate spacing on mobile
   - Ensure the container matches standard pattern: `max-w-[1200px] mx-auto px-6 md:px-12 lg:px-0`

4. Fix `components/CTA.tsx`:
   - Read the file first to check current layout
   - Ensure the container matches standard pattern
   - Ensure CTA buttons have min-h-[44px] for touch targets on mobile

5. Fix `components/CalendlyButton.tsx`:
   - Add `min-h-[44px]` to the button styling to ensure adequate touch target on mobile devices
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "pt-24" components/Hero.tsx
    - grep -q "max-w-\[1200px\]" components/Services.tsx
    - grep -q "min-h-\[44px\]" components/CalendlyButton.tsx
    - grep -q "max-w-\[320px\]" components/Hero.tsx
  </acceptance_criteria>
  <done>Hero has proper top padding for fixed nav, image scales on mobile, all containers use consistent padding, CTA touch targets meet 44px minimum</done>
</task>

<task type="auto">
  <name>Task 2: Polish forms, ExpertiseSection, and remaining components for mobile</name>
  <read_first>components/ContactForm.tsx, components/LeadMagnet.tsx, components/ExpertiseSection.tsx, components/WorkflowShowcase.tsx, components/About.tsx, components/ProblemSection.tsx, components/ResultsSection.tsx, components/TrustBar.tsx</read_first>
  <files>components/ContactForm.tsx, components/LeadMagnet.tsx, components/ExpertiseSection.tsx, components/WorkflowShowcase.tsx</files>
  <action>
1. Polish `components/ContactForm.tsx` for mobile (per CONV-02):
   - Ensure all input fields have `min-h-[44px]` for touch friendliness (current py-3 px-4 likely achieves ~44px but make it explicit)
   - Ensure the select dropdown has the same min-height
   - Add `text-base` to all inputs to prevent iOS zoom on focus (iOS zooms if input font-size < 16px)
   - Ensure submit button has `min-h-[44px]`

2. Polish `components/LeadMagnet.tsx` for mobile (per CONV-02):
   - The form card (`bg-muted rounded-xl p-8`) has p-8 on mobile which may be tight. Change to `p-6 md:p-8`.
   - Ensure the email input and submit button have `min-h-[44px]` and `text-base` for iOS zoom prevention

3. Polish `components/ExpertiseSection.tsx` for mobile (per CONV-02):
   - The grid is `grid-cols-1 md:grid-cols-2 lg:grid-cols-3` which is already good
   - Verify Card components render well on small screens (they should since they're full-width at mobile)

4. Polish `components/WorkflowShowcase.tsx` for mobile (per CONV-02):
   - The grid is `grid-cols-1 md:grid-cols-2` which is already good
   - Verify card content doesn't overflow on narrow screens

5. For components About.tsx, ProblemSection.tsx, ResultsSection.tsx, TrustBar.tsx — read and verify they follow the standard container pattern. Fix if they use inconsistent padding. These are simpler components so issues are less likely, but check for:
   - Container pattern: `max-w-[1200px] mx-auto px-6 md:px-12 lg:px-0`
   - No fixed widths that would cause horizontal overflow
   - Text sizes are readable on mobile

Only modify files that actually need changes. If a component already works well on mobile, leave it alone.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "text-base" components/ContactForm.tsx
    - grep -q "p-6 md:p-8" components/LeadMagnet.tsx
    - npm run build exits successfully
  </acceptance_criteria>
  <done>All form inputs prevent iOS zoom (text-base), touch targets are 44px+, LeadMagnet form card has responsive padding, all components verified for mobile layout, build succeeds</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- No horizontal overflow on any section (verified by build success and code review)
- All containers use consistent `px-6 md:px-12 lg:px-0` padding pattern
- All form inputs have `text-base` class (prevents iOS zoom)
- All interactive elements have minimum 44px touch target
</verification>

<success_criteria>
- All sections render correctly on 375px viewport width
- Hero image scales appropriately on mobile
- Hero content is not hidden behind fixed navigation
- Contact form and lead magnet form are usable on mobile (no iOS zoom, adequate touch targets)
- Consistent horizontal padding across all sections
- Service cards stack to single column on mobile
- Build succeeds cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/09-mobile-responsive-polish/09-02-SUMMARY.md`
</output>
